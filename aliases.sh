#!/bin/bash
##
# Aliases for scripts in `scripts/` are prefixed with mt and copied to `~/.bin`.
#

alias virtualenc-enable='. ~/.bin/env-activate.sh'

alias ssh-agent-enable='~/.bin/ssh-agent.sh'

alias dev-watch='~/.bin/monitor.sh'
